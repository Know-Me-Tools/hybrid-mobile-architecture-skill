## 1. Publication dependencies and integration

- [ ] 1.1 Add pinned Ajv, Linkinator, Playwright Test, and `@axe-core/playwright`
  dependencies, scripts, browser-cache policy, and lockfile updates without changing
  the proven Docusaurus/local-search versions.
- [ ] 1.2 Configure the prompting plugin, nested autogenerated sidebars, local search,
  sitemap, and route inventory for every canonical start/model/harness/loop/scenario/
  case-study/verification page.
- [ ] 1.3 Preserve KnowMe branding and Flat 2.0 light/dark styling on all new page,
  prompt, code, table, Mermaid, search, and navigation states.

## 2. Deterministic release gates

- [ ] 2.1 Wire sanitizer, generated-routing drift, Ajv, semantic, skill-resolution,
  architecture, source/freshness, and required-route checks into one fail-fast command.
- [ ] 2.2 Add built-output, sitemap, and user-observable local-search assertions for all
  required stable routes and ten recipe IDs.
- [ ] 2.3 Adapt Linkinator into separate fatal internal and bounded external checks with
  explicit timeout, retry, status policy, and reviewed allowlist fixtures.

## 3. Browser and accessibility evidence

- [ ] 3.1 Add Playwright web-server/projects configuration for representative start,
  harness, loop, scenario, model, and case-study routes at desktop/mobile sizes and
  light/dark themes.
- [ ] 3.2 Test navigation, search, prompt copy and accessible status, Mermaid, responsive
  layout, keyboard/focus order, and absence of default branding/private content.
- [ ] 3.3 Integrate axe scans for configured-impact failures and record manual keyboard,
  contrast, Flat 2.0, and screenshot review evidence.

## 4. CI and clean-build proof

- [ ] 4.1 Extend the pinned Pages workflow so deterministic, build, route, link,
  Playwright, and axe gates pass before artifact upload; retain static-only ownership
  and full-SHA action pins.
- [ ] 4.2 Run frozen install, all gates, and production build from a fresh clone with no
  ignored site output or caches; serve representative routes and verify search/social
  metadata from the built artifact.

## 5. Representative harness certification

- [ ] 5.1 Exercise the full-hybrid recipe in Codex within its declared authority and
  retain a sanitized evidence manifest covering artifacts, recovery, public-boundary
  observation, independent critic, and Karpathy record.
- [ ] 5.2 Select one installed authenticated ACP/CLI harness from OpenCode, Kimi, or Zed,
  exercise a representative recipe, and retain the same sanitized evidence contract.
- [ ] 5.3 Run the orchestration skill against a scratch project, validate all public
  routes and release evidence, record the private/project Karpathy phase closure, and
  certify only when no required gate remains incomplete.
