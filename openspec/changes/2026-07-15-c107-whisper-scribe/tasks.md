# Tasks — 2026-07-15-c107-whisper-scribe

- [x] T1 — `gen_ui_audio` crate: whisper-rs + whisper-tiny on-device transcription
      (`src/transcribe.rs`).
- [x] T2 — Fixed five whisper-rs/cpal API-version mismatches surfaced when the crate
      was integrated into the main checkout.
- [x] T3 — FFI surface: `gen_ui_ffi::api::scribe` (`scribe_start`/`scribe_stop`),
      literal `Result<T, CoreError>` signatures (frb generic-alias-blindness fix
      applied consistently with the rest of the FFI surface).
- [x] T4 — Tauri commands + autogenerated permissions
      (`tauri-plugin-gen-ui/permissions/autogenerated/commands/scribe_{start,stop}.toml`).
- [x] T5 — Android target-gate: `gen_ui_audio`/whisper-rs disabled on Android pending
      a CMake ABI-detection fix (documented gap, non-blocking — desktop/iOS unaffected).
- [x] T6 — Verified: `cargo clippy --workspace --all-targets -- -D warnings` clean
      across the full workspace including `gen_ui_audio`.
