Implemented C-003 by modifying `scripts/scaffold-rust-core.sh` to generate the feature-gated `gen_ui_db::relational` crate with SQLx PostgreSQL/cloud and pglite-oxide desktop support, SQLx SQLite plus sqlite-vec registration for mobile, additive per-dialect migrations, typed library errors, bundled/HTTP/IPFS seed sources, and a typestate startup orchestrator that enforces migrations before seeds and sync attachment; the generated crate also includes one integration-test binary with three public-boundary behavior tests. Verification passed with `bash -n`, `git diff --check`, SQLite and PGlite/PostgreSQL `cargo clippy` feature gates under `-D warnings`, and all 3 SQLite boundary tests. Deviations: the installed OpenSpec CLI cannot address date-prefixed change IDs, the named `postgres-patterns` skill was absent from the shared skill pack, SQL migration files use the dialect-valid `-- TJ-ARCH-MOB-001 compliant` header instead of `//`, and pglite-oxide 0.5.1 required an exact `virtual-net = 0.702.0-alpha.3` compatibility pin; there are no remaining blockers.
